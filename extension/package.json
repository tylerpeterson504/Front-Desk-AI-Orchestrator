{
  "name": "front-desk-ai-extension",
  "version": "1.0.0",
  "description": "Front Desk AI Chrome Extension",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest",
    "test:watch": "vitest watch",
    "lint": "eslint src --ext .js,.ts",
    "lint:fix": "eslint src --ext .js,.ts --fix"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3",
    "vite": "^5.3.3",
    "vitest": "^1.6.0",
    "jsdom": "^25.0.1"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFiles": [
      "./tests/setup.js"
    ],
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
